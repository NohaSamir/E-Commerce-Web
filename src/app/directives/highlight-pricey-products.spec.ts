import { HighlightPriceyProducts } from './highlight-pricey-products';

describe('HighlightPriceyProducts', () => {
  it('should create an instance', () => {
    const directive = new HighlightPriceyProducts();
    expect(directive).toBeTruthy();
  });
});
